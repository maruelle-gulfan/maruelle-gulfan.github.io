# Drop project screenshots here to override the live-fetched ones.
#
# Usage:
#   1. Save your image as e.g. `reunirgracias.png` in this folder.
#   2. In `src/lib/content.ts`, set `image: "/projects/reunirgracias.png"`
#      on that project entry.
#
# Recommended: 1280x800 PNG/WebP/JPG, under ~200 KB.
